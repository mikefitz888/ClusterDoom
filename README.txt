bin: output executables
build: object files, emptied on clear
doc: docs
include: project header files + 3rd party header files
lib: any libraries that get compiled by the project
src: source files
test: so it looks like we have unit tests